using System;

namespace homework2
{
    internal class Program
    {
        private static int count;
       
        static void Main(string[] args)
        {
           
            Console.WriteLine(count);
        }
        static string VowelsLetter(string[] letter)
        {

            string text = "Salam Aleykum";
            Type vowelscount = null;
            const vowels = null;

            for (int i = 0; i < letter.Length; i++) 
            {
                if (text =[text[i] == 'a' || text[i] == 'e' || text[i] == 'i' || text[i] == 'o' || text[i] == 'u' || text[i] == 'ö' || text[i] == 'ü' || text[i] == 'ı' || text[i] == 'ə']) ;


                else

                    count++;
            }


        }
    }
}
